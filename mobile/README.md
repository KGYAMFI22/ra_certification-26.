# Mobile App

React Native mobile application for RA Compliance Assistant.

## Setup
1. Install Node.js and npm
2. npm install
3. For iOS: cd ios && pod install
4. Run: npx react-native run-ios/android
